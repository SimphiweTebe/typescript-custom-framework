{
  "scripts": {
    "start:parcel": "npx parcel index.html",
    "start:db": "npx json-server -w db.json --port 4000",
    "build": "npx build index.html"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}
